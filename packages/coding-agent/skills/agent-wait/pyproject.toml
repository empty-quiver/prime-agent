[project]
name = "agent-wait"
version = "0.1.0"
description = "Durable session waits through the Prime Agent host bridge"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agent_wait"]
